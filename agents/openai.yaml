interface:
  display_name: "PDF to Agent Markdown"
  short_description: "Vision-first PDFs to faithful Agent-readable Markdown"
  default_prompt: "Use $pdf-to-agent-markdown to transcribe this complete PDF into faithful, Agent-readable Markdown with conservative formulas, PDF page traceability, progressive long-document handling, and necessary visual retention."

policy:
  allow_implicit_invocation: true
